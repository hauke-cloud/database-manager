{{- if .Values.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "database-manager.fullname" . }}-leader-election-rolebinding
  labels:
    {{- include "database-manager.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "database-manager.fullname" . }}-leader-election-role
subjects:
- kind: ServiceAccount
  name: {{ include "database-manager.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
{{- end }}
